# Copy to .env.local and fill in from your Supabase project
# (Project Settings → API). Leaving these unset keeps the app fully
# local-first (no login, full offline use).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
